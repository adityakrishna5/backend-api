<configuration>
  <springProfile name="local">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
      </encoder>
    </appender>
    <root level="DEBUG"><appender-ref ref="CONSOLE"/></root>
  </springProfile>

  <springProfile name="gcp">
    <!-- Structured JSON → STDOUT → New Relic Fluent Bit picks up automatically -->
    <appender name="JSON_STDOUT" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <customFields>{"service":"${spring.application.name:-unknown}","env":"gcp","app":"store"}</customFields>
        <fieldNames>
          <timestamp>timestamp</timestamp>
          <message>message</message>
          <logger>logger</logger>
          <level>level</level>
        </fieldNames>
        <includeMdcKeyName>X-Correlation-ID</includeMdcKeyName>
        <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
          <maxDepthPerCause>10</maxDepthPerCause>
        </throwableConverter>
      </encoder>
    </appender>
    <root level="WARN"><appender-ref ref="JSON_STDOUT"/></root>
    <logger name="com.store" level="INFO"/>
  </springProfile>
</configuration>
