apiVersion: v1
kind: Service
metadata:
  name: api-rw
  namespace: platform
  labels:
    app: api-rw
spec:
  type: ClusterIP
  selector:
    app: api-rw
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
