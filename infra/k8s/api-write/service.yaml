apiVersion: v1
kind: Service
metadata:
  name: api-write
  namespace: platform
  labels:
    app: api-write
spec:
  type: ClusterIP
  selector:
    app: api-write
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
