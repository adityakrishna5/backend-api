apiVersion: v1
kind: Service
metadata:
  name: api-read
  namespace: platform
  labels:
    app: api-read
spec:
  type: ClusterIP
  selector:
    app: api-read
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
